# Django
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=
LOG_LEVEL=INFO

# 알림 발송용 슬랙 봇 토큰 (xoxb-...)
SLACK_TOKEN=

# 비워두면 API가 인증 없이 열린다. 외부에 노출할 때는 반드시 채울 것
JERRYBOT_API_KEY=

CRAWLER_USER_AGENT=

# 비워두면 SQLite(db.sqlite3). 서버에서는 Postgres 주소를 넣는다
DATABASE_URL=

# 관리자 화면(/admin/) 계정. 둘 다 있을 때 기동 시 자동 생성(이미 있으면 유지)
DJANGO_SUPERUSER_USERNAME=
DJANGO_SUPERUSER_PASSWORD=
